var Blocks; 
if (Blocks === undefined) {
    Blocks = {};
}

var game = BLOCKS.eventDispatcher(),
var listener, dispatcher;
dispatcher = BLOCKS.eventListener();listener.addEventListener('fire', eventFired);dispatcher.dispatchEvent('fire');